<!doctype html>
<html prefix="og: http://ogp.me/ns#">
  <head>
    <!-- OBSERVATORIO SEMANTIC VERSION 1.0.1 -->
    <meta charset="utf-8">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta name="viewport"
          content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Use the title from a page's frontmatter if it has one -->
    <title><%= current_page.data.title || "Project Name" %></title>
    <meta name="description" content="<%= current_page.data.metaDescription || 'Observatorio' %>">


    
    <% if ( current_page.data.ogTitle ) %>
        <meta property="og:site_name" content="<%= current_page.data.title || 'Observatorio' %>"/>
        <meta property="og:image" content="<%= config[:host] %><%= config[:images_dir] %>/content/<%= current_page.data.ogImage %>"/>
        <meta property="og:url" content="<%= config[:host] %><%= current_page.data.ogUrl %>"/>
        <meta property="og:type" content="article" />
        <meta property="og:title" content="<%= current_page.data.ogTitle %>"/>
    <% end %>
    <% if ( current_page.data.ogDescription ) %>
        <meta property="og:description" content="<%= current_page.data.ogDescription %>"/>
    <% end %>

    <%= stylesheet_link_tag("stylesheets") %>
    <script>
        window.DigitalData = window.DigitalData || [];
          (function(w, d, s, l, i) {
            w[l] = w[l] || []; w[l].push({
            'gtm.start':
            new Date().getTime(), event: 'gtm.js'
           });
            var f = d.getElementsByTagName(s)[0],
            j = d.createElement(s), dl = l != 'dataLayer' ? '&l=' + l : ''; j.async = true; j.src ='https://www.googletagmanager.com/gtm.js?id='+ i + dl; f.parentNode.insertBefore(j, f);
       })(window,document, 'script', 'dataLayer', 'GTM-W99PG5T');
    </script>
    <script src="//assets.adobedtm.com/launch-EN2267055cc3114071b44e48093e6f41e3.min.js"></script>
    <script>
     
        var d = new Date();
        var n = d.getDay();
        var days = ['Domingo','Lunes', 'Martes','Miercoles','Jueves','Viernes','Sabado'];
        var weekDay = days[ n ];
        var typeOfDay = ( n == 0 || n == 6 ) ? 'weekend':'week' ;
        function deviceOS() {
          var useragent = navigator.userAgent;
          
          if(useragent.match(/Android/i)) {
                return 'mobile';  //return 'android';
            } else if(useragent.match(/webOS/i)) {
                return 'mobile';  //return 'webos';
            } else if(useragent.match(/iPhone/i)) {
                return 'mobile';  //return 'iphone';
            } else if(useragent.match(/iPod/i)) {
                return 'mobile';  //return 'ipod';
            } else if(useragent.match(/iPad/i)) {
                return 'mobile';  //return 'ipad';
            } else if(useragent.match(/Windows Phone/i)) {
                return 'mobile';  //return 'windows phone';
            } else if(useragent.match(/SymbianOS/i)) {
                return 'mobile';  //return 'symbian';
            } else if(useragent.match(/RIM/i) || useragent.match(/BB/i)) {
                return 'mobile';  //return 'blackberry';
            } else {
                return 'desktop';
            }
        }

        var device = deviceOS();
        var date = new Date();
        var localeTime =  d.toLocaleTimeString();
    </script>
    <%= javascript_include_tag("all.fn", "all") %>
  </head>
  <body>
    <div class="body-wrapper">
      <%= yield %>
    </div>
  </body>
</html>
